<h1><%= @ticket.title %></h1>

<%= image_tag @work.image, :alt => @work.title, :class => 'feature' %>

<ul>
  <li>Title: <%= @work.title %></li>
  <% if @work.artist.present? %>
    <li>Artist: <%= link_to @work.artist.name, @work.artist %></li>
  <% end %>
  <li>Year: <%= @work.year %></li>
  <li>Medium: <%= @work.medium %></li>
  <li>Style: <%= @work.style %></li>
</ul>

<div class="controls">
  <%= link_to 'Edit Ticket', edit_ticket_path(@ticket) %>
  <%= link_to 'Delete work', @ticket, :method => 'delete', :data => { :confirm => 'Are you sure?' } %>
</div>
